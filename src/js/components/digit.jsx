import React from "react";

const Digit = (props) => {
    
    return (
        <>
        <span style={{backgroundColor: "#101010", color: "white", fontSize: "100px", padding:"10px", marginTop: "14px", marginBottom: "14px" }}>{props.number}</span>
        </>
    )
}

export default Digit 